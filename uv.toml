# uv configuration for acoharmony project
# Force uv to always use Python 3.13
python-preference = "only-managed"

# Publishing configuration for GitHub Packages (Python/wheel)
# Use: uv publish --username __token__ --password <GITHUB_TOKEN>
publish-url = "https://ghp.pkg.github.com/acoharmony"
